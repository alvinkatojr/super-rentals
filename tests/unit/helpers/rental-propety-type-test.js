
import { rentalPropetyType } from 'super-rentals/helpers/rental-propety-type';
import { module, test } from 'qunit';

module('Unit | Helper | rental propety type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPropetyType([42]);
  assert.ok(result);
});

